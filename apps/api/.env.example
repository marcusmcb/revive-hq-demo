# Express API (local development)

# Server
PORT=4000

# CORS
# Set to your web origin when running locally (no trailing slash).
# Example: http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# Repliers provider (required)
REPLIERS_API_KEY=YOUR_REPLIERS_API_KEY

# Optional (defaults to https://api.repliers.io)
REPLIERS_API_BASE_URL=https://api.repliers.io

# Firebase Admin / Firestore (choose ONE approach)
# Option A: full service account JSON in an env var (paste JSON on one line)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Option B: path to a downloaded service account file
#
# Common local setup:
# 1) Create a folder at the repo root: ./json
# 2) Put your service account file there (this repo gitignores json/)
# 3) Set the path relative to apps/api (note the ../../)
# FIREBASE_SERVICE_ACCOUNT_PATH=../../json/service-account.json

# Option C: rely on GOOGLE_APPLICATION_CREDENTIALS (and optionally set project)
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json
# FIREBASE_PROJECT_ID=your-firebase-project-id
