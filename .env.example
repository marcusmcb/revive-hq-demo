# This repo is an npm workspaces monorepo.
# For local development, prefer copying these values into:
# - apps/web/.env.local (frontend)
# - apps/api/.env (api)
#
# This root file exists mainly to satisfy "provide .env.example" deliverables.

# Frontend (Next.js)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Backend (Express API)
PORT=4000
CORS_ORIGIN=http://localhost:3000
REPLIERS_API_KEY=YOUR_REPLIERS_API_KEY
REPLIERS_API_BASE_URL=https://api.repliers.io

# Firebase Admin / Firestore (choose ONE approach)
# Option A: full service account JSON in an env var
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Option B: path to a service account JSON file
# If you store it under the repo-root ./json folder, then from apps/api the path is:
# FIREBASE_SERVICE_ACCOUNT_PATH=../../json/service-account.json

# FIREBASE_PROJECT_ID=your-firebase-project-id
